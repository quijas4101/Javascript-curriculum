//1.you can't define a variable more than once
let petName = 'Hal'  // define variable petName

petName = 'Spot'  // reassigning petName to equal Spot

console.log(petName)

// 2. There are rules related to variables
/// must start with letters, $, or _
/// only symbols allowed are _ or $

//3. Variables names cannot be reserved keywords